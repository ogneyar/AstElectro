
// фильтр по цене, от и до
function filterPrice(data) {
    // data = data.filter(i => i.price >= productStore.filter.price[0] && i.price <= productStore.filter.price[1])
}

module.exports = filterPrice